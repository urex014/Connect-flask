li.innerHTML = `
    <a href="/chat/${user.id}" class="chat-link">
        <div class="chat-info">
            <img src="/static/uploads/${user.profile_photo}" alt="${user.username}'s Profile Picture" class="profile-photo">
            <div class="message-details">
                <p class="username">${user.username}</p>
            </div>
        </div>
    </a>
`;